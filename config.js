/*
  Production configuration.
  The public Solana RPC is intentionally the default so the project runs immediately.
  For production traffic, replace rpcUrl with a dedicated Solana endpoint from your provider.
*/
window.RHC_FACTORY_CONFIG = {
  rpcUrl: 'https://api.mainnet-beta.solana.com'
};
